spring:
  application:
    name: hotel-reservation

  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/hotel_reservation}

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:grandreserve9@gmail.com}
    password: ${MAIL_PASSWORD:bekkkfgmcexhblma}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

server:
  port: ${SERVER_PORT:8082}

jwt:
  secret: ${JWT_SECRET:DefaultDevSecretKeyThatIsLongEnoughForHS256AlgorithmOnly}
  expiration: ${JWT_EXPIRATION:86400000}

app:
  mail:
    from: ${MAIL_FROM:grandreserve9@gmail.com}
    admin-to: ${MAIL_TO:grandreserve9@gmail.com}
